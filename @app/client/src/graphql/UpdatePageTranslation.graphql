mutation UpdatePageTranslation(
  $content: String!
  $pageName: String!
  $languageCode: TrLanguage!
) {
  updatePageTr(
    input: {
      patch: { content: $content }
      pageName: $pageName
      languageCode: $languageCode
    }
  ) {
    pageTr {
      ...PageTranslation
    }
  }
}
