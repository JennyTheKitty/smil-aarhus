query PageQuery($name: String = "") {
  page(name: $name) {
    name
    translations {
      nodes {
        ...PageTranslation
      }
    }
  }
}

fragment PageTranslation on PageTr {
  __typename
  content
  languageCode
  pageName
}
