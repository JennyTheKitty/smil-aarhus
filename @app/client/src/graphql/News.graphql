query NewsesQuery {
  newses(orderBy: PUBLISHED_AT_DESC) {
    nodes {
      publishedAt
      id
      updatedAt
      translations {
        nodes {
          content
          title
          languageCode
          newsId
        }
      }
    }
  }
}
